import styled from '@emotion/styled'
import { Box, Menu, Typography } from '@mui/material'

export const ChainSwitcherContainer = styled(Menu)(({ theme }) => ({
  borderRadius: theme.shape.borderRadius,
}))
